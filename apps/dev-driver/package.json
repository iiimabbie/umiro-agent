{
  "name": "@umiro/dev-driver",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@umiro/core": "workspace:*",
    "@umiro/model-openai": "workspace:*",
    "@umiro/storage-sqlite": "workspace:*"
    ,"@umiro/gateway": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "pnpm build && node --test dist/test/*.test.js",
    "dev": "pnpm build && node --env-file=../../.env dist/src/main.js"
  }
}
