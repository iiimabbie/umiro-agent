{
  "name": "umiro",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.3.4",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "tsc --build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @umiro/dev-driver dev",
    "umo": "pnpm --filter @umiro/cli start --"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
